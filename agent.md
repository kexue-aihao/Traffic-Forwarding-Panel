# Agent Instructions

- 后续交互统一使用中文输出。
- 代码编写和部署假设目标环境为 Debian 系统。
- 不要求在本地环境进行测试、编译或 Docker 构建验证；本地环境可能与目标环境不一致。
- 修改代码后可以直接提交到 GitHub，由 GitHub Actions 负责执行编译和测试并反馈错误。
- 如果需要验证，优先根据 GitHub Actions 日志进行修复，而不是强依赖本地测试结果。
- 仍需保持代码改动可审查、范围清晰，并说明预期由 CI 验证的内容。
