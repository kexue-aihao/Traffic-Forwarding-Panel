# v0.1.14

探针页面的状态标现在带标记：在线是绿底白勾，离线是红底白叉，数据陈旧是黄底白钟。此前离线只是灰底方块，既没有标记，也用同一种底色盖住了「没上报」和「已离线」这两种不同的情况。

IPv4 与 IPv6 的位置图标从地址上方挪到地址前面、同一行显示，位置查询仍走站点设置里的地区查询地址，留空即关闭；地区码认不出时地址格退回带码的中性徽章。功能范围与 v0.1.13 相同，chromium/firefox/webkit 浏览器测试与真实 Go/SQLite 后端集成测试均通过。
