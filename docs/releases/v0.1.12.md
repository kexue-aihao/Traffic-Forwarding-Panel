# v0.1.12

Agent 默认每 10 分钟通过固定的 `curl` 命令向 `https://api.ipify.org` 探测公网 IPv4 和 IPv6 地址。IPv6 不可用时继续上报 IPv4；命令失败不会阻止 Agent 运行。配置 `-ip-echo` 时仍使用运营方提供的 HTTPS 回显服务。

公网地址响应现在会校验为公网单播 IP，Linux Agent 安装器也会在安装前检查 `curl` 是否可用。新增探测逻辑的单元测试覆盖 IPv6 失败、无效输出、私网地址和自定义回显服务。
