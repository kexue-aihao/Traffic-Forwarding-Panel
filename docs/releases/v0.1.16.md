# v0.1.16

探针页面的完整 IPv6 地址此前被省略号截断。一列只有 96px 能放地址，而 38 个字符的地址需要 230px，页面上只能看到前 14 个字符 —— 剩下的部分认不出是哪一台机器，而地址正是这一列的全部内容。现在地址列加宽（IPv4 140px、IPv6 180px，卡片、卡片头与数据行的最小宽度同步到 1200px），地址改为折行显示，IPv4 仍然占一行。

功能范围与 v0.1.15 相同，chromium/firefox/webkit 浏览器测试与真实 Go/SQLite 后端集成测试均通过。
