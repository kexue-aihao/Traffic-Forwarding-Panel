# v0.1.17

实时探针改为独立窗口。侧栏的「实时探针」与概览页的入口现在都新开一个浏览器标签页，标签页里没有侧栏与顶栏，探针内容铺满整屏（该页不再受 1400px 的页面宽度上限约束）。直接访问 `#/probes` 同样是独立布局，外壳里不再保留任何嵌入式版本。

独立窗口没有侧栏，因此那个标签页里没有页内导航，关掉标签页即回到原来的页面。此改动只涉及面板前端，不需要更新节点上的 Agent。功能范围与 v0.1.16 相同，chromium/firefox/webkit 浏览器测试与真实 Go/SQLite 后端集成测试均通过。
