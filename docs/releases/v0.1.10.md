# v0.1.10

Docker 安装入口现在通过无参数菜单提供首次安装、升级到最新正式版和重置密码，密码与确认输入通过控制终端交互。

在线安装和升级固定从 GitHub Latest Release 获取安装器，避免同目录旧版脚本让升级停留在旧版本。升级前仍会校验镜像、备份安装目录并验证新容器；离线包继续使用包内匹配版本的安装器。

运行以下命令后，在菜单中选择所需操作：

```sh
curl -fsSL https://github.com/kexue-aihao/Traffic-Forwarding-Panel/raw/master/install.sh | bash
```
